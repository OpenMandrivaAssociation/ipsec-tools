--- ipsec-tools-0.5.2/src/racoon/missing/crypto/sha2/sha2.c.includes	2004-09-21 16:35:25.000000000 +0200
+++ ipsec-tools-0.5.2/src/racoon/missing/crypto/sha2/sha2.c	2005-09-07 13:17:41.000000000 +0200
@@ -50,8 +50,12 @@
 
 #include <err.h>
 #include <string.h>
+#ifndef bcopy
 #define bcopy(a, b, c) memcpy((b), (a), (c))
+#endif
+#ifndef bzero
 #define bzero(a, b) memset((a), 0, (b))
+#endif
 #define panic(a) err(1, (a))
 
 #if OPENSSL_VERSION_NUMBER >= 0x00907000L
